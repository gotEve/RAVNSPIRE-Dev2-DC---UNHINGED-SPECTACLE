name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to Oracle Cloud Server
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: ${{ secrets.ORACLE_HOST }}
        username: ${{ secrets.ORACLE_USERNAME }}
        key: ${{ secrets.ORACLE_SSH_KEY }}
        port: 22
        timeout: 10m
        command_timeout: 8m
        script: |
          # Navigate to bot directory
          cd /home/ubuntu/discord-bots/ravnspire
          
          # Pull latest changes
          echo "Pulling latest changes from GitHub..."
          git pull origin main
          
          # Install any new dependencies
          echo "Installing dependencies..."
          npm install
          
          # Deploy slash commands
          echo "Deploying slash commands..."
          node deploy-commands.js
          
          # Restart the bot with PM2
          echo "Restarting bot..."
          pm2 restart ravnspire-bot
          
          # Check bot status
          echo "Checking bot status..."
          pm2 status
          
          # Wait a moment for bot to fully initialize
          echo "Waiting for bot to initialize..."
          sleep 10
          
          # Check if bot is ready
          echo "Checking bot logs for successful startup..."
          timeout 5 pm2 logs ravnspire-bot --lines 5 || true
          
          echo "Deployment completed successfully!"
